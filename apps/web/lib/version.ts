// Surfaced in the sidebar. Bump with each meaningful commit.
// Versioning starts at 0.0.0; current value is the version after the
// most recent commit.
import pkg from "../package.json";
export const PLEXUS_VERSION: string = pkg.version;
